// Condition
// TRUE FALSE, 1 0
// > >= < <= === !
// && , ||
// T && T = T,
// F || F = F

let a = 11;
let b = 2;

if (a > 10) {
    console.log('TRUE');
} else {
    console.log('FALSE');
}

// กำหนดค่า a, b และหาค่า max
// เช่น a = 1, b = 2, ผลลัพธ์ max = 2